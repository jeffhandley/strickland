{
  "name": "strickland-root",
  "private": true,
  "scripts": {
    "demo": "cd demo && npm start",
    "prepublish": "cp README.md strickland && cp CHANGELOG.md docs",
    "publish": "lerna publish --conventional-commits",
    "publish:prerelease": "npm run publish -- --cd-version=prerelease",
    "postinstall": "lerna bootstrap",
    "test": "lerna run test",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^2.5.1"
  }
}
