{
  "name": "strickland-root",
  "private": true,
  "scripts": {
    "demo": "cd demo && npm start",
    "prepublish": "cp README.md strickland",
    "publish": "lerna publish --conventional-commits",
    "postinstall": "lerna bootstrap",
    "test": "lerna run test",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^2.5.1"
  }
}
